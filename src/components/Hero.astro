---
---

<section id="hero" class="relative min-h-screen flex items-center justify-center bg-dark-950 overflow-hidden">
  <!-- Background gradient glow -->
  <div class="absolute inset-0">
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_rgba(30,58,95,0.3)_0%,_rgba(10,10,10,0)_70%)]"></div>
    <!-- Subtle dot pattern overlay -->
    <div class="absolute inset-0 opacity-[0.03]" style="background-image: radial-gradient(circle, #c0c0c0 1px, transparent 1px); background-size: 30px 30px;"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 text-center px-4 sm:px-6 max-w-4xl mx-auto">
    <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white mb-6 tracking-tight">
      Precision i varje yta
    </h1>
    <p class="text-lg sm:text-xl md:text-2xl text-steel-400 max-w-3xl mx-auto mb-10 leading-relaxed">
      Vi ger metall nytt liv genom vattenpolering — den skonsamma ytbehandlingen som levererar resultat i världsklass.
    </p>
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <a href="#galleri" class="inline-flex items-center gap-2 bg-water-500 hover:bg-water-400 text-white font-semibold px-8 py-4 rounded-lg transition-all duration-300 hover:shadow-lg hover:shadow-water-500/25 text-lg">
        Se våra resultat
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
      </a>
      <a href="#kontakt" class="inline-flex items-center gap-2 border border-steel-500 hover:border-silver-300 text-silver-200 hover:text-white font-semibold px-8 py-4 rounded-lg transition-all duration-300 text-lg">
        Kontakta oss
      </a>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2">
    <div class="w-6 h-10 border-2 border-steel-500 rounded-full flex justify-center">
      <div class="w-1.5 h-3 bg-steel-400 rounded-full mt-2 animate-bounce"></div>
    </div>
  </div>
</section>
